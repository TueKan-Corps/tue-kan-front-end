/*
  .add in 02-Apr-20
    -- using for centralized account_id.
*/

import React from 'react';

/*const SetAccountData =(props)=> {
  const [accountId, setAccountId] = React.useState(0);

  setAccountId(props.id);
  
  return accountId;
}

export default SetAccountData;*/

export const accountData = {
  account_id: 29,
}
